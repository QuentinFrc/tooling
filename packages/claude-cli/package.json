{
  "name": "claude-cli",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "claude-storage": "./dist/bin.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "link:install": "pnpm build && npm i -g ."
  },
  "dependencies": {
    "@clack/prompts": "^0.10",
    "commander": "^13",
    "execa": "^9"
  },
  "devDependencies": {
    "tsup": "^8",
    "typescript": "^5.7"
  }
}
